# Contributing

1. Keep the single-URL harvest path working before adding new pages.
2. Do not commit `node_modules`, `media/harvests`, or secrets.
3. Run `python manage.py test` from the repo root when you touch backend code.
